# Consumer ProGuard rules for the :settings module.
# DataStore Preferences and Compose are kept by their own consumer rules pulled in
# transitively; nothing module-specific needs to be kept for consumers.
